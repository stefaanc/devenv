---

apiVersion: v1
kind: Service
metadata:
  name: atlas
  namespace: atlas
  labels:
    app: atlas
spec:
  type: ClusterIP
  ports:
  - 
    name: http
    port: 8080
    protocol: TCP
     targetPort: http
 selector:
    app: atlas
    release: atlas-r1
